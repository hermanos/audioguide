<nav>
  <ul class="nav nav-pills">
    <li>
      <%= link_to 'Home', dashboard_path %>
    </li>
    <% if user_signed_in? %>
       <li>
          <%= link_to "Manage the Achievements", achievements_path %>  
        </li>
        <li>
          <%= link_to "Manage your Account", edit_user_registration_path %>  
        </li>
        <li>
          <%= link_to "Manage the Museums", museums_path %>  
        </li>
        <li>
          <%= link_to "Sign Out", destroy_user_session_path, method: :delete %> 
        </li>
    <% else %>
          <% unless current_page?(new_user_session_path) or 
          current_page?(new_user_registration_path) or current_page? (new_user_password_path) %>
          <li>
          <%= link_to "Sign In", new_user_session_path %>  
          </li>
          <li>
           <%= link_to "Sign Up", new_user_registration_path %>  
          </li>
    <% end %>
    <% end %>
         
  </ul>
</nav>
